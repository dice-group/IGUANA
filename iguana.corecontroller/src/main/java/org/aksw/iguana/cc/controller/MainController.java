package org.aksw.iguana.cc.controller;

public class MainController {

	
	/* TODO 
	 * - read configuration
	 * - start Webservice to get TaskConfigurations
	 * - start DataGenerator if needed
	 * - start TaskGenerator 
	 * - if TG is finished start next Task in Configuration
	 * - 	repeat until all Tasks are finished
	 * - do this all day long!
	 */		
}
